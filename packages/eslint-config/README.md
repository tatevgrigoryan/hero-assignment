# @repo/eslint-config

Shared ESLint configuration presets for the monorepo.

## Exports

- `@repo/eslint-config/base` - baseline TypeScript + React rules.
- `@repo/eslint-config/nextjs` - Next.js 16 + React 18 rules with packages-only imports enforced for apps.
